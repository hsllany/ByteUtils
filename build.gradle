apply plugin: 'java'

version = '1.1';

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}
jar {
    manifest {
        attributes 'Implementation-Title': 'ToByteUtils',
                   'Implementation-Version': version
    }
}